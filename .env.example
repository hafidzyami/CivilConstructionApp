# Environment variables for docker-compose
# Copy this file to .env and fill in your values

# PostgreSQL Configuration (connecting to existing host PostgreSQL)
# Use credentials from your existing PostgreSQL installation on the VM
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=civilconstruction
POSTGRES_PORT=5432

# Backend Configuration
BACKEND_PORT=6969
NODE_ENV=production

# Frontend Configuration
FRONTEND_PORT=6968
NEXT_PUBLIC_API_URL=http://localhost:6969/api
